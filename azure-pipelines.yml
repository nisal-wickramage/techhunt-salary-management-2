# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  vmImage: ubuntu-latest

variables:
  buildConfiguration: 'Release'
  Major: '1'
  Minor: '0'
  Patch: '0'

steps:
- task: DotNetCoreCLI@2
  inputs:
    command: publish
    publishWebProjects: True
    arguments: '--configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: True
    projects: 'src/Techhunt.SalaryManagement.Api/Techhunt.SalaryManagement.Api.csproj'
    
- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Build.ArtifactStagingDirectory)' 
    artifactName: 'api-app'
# - task: NuGetCommand@2
#   inputs:
#     command: pack
#     versioningScheme: byBuildNumber
#     majorVersion: '$(Major)'
#     minorVersion: '$(Minor)'
#     patchVersion: '$(Patch)'
#     packagesToPack: 'src/Techhunt.SalaryManagement.Api/Techhunt.SalaryManagement.Api.csproj'
#     packDestination: '$(Build.ArtifactStagingDirectory)'
#     includeReferencedProjects: true

# - task: NuGetAuthenticate@0
#   displayName: 'NuGet Authenticate'
# - task: NuGetCommand@2
#   displayName: 'NuGet push'
#   inputs:
#     command: push
#     publishVstsFeed: 'serverlesshub/wep-app-001'
#     allowPackageConflicts: true